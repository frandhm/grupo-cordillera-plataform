version: '3.8'
services:
  # Base de Datos de KPIs
  db-kpis:
    image: postgres:15
    container_name: cordillera-db-kpis
    environment:
      POSTGRES_USER: user_kpis
      POSTGRES_PASSWORD: password_kpis
      POSTGRES_DB: db_kpis
    ports:
      - "5432:5432"